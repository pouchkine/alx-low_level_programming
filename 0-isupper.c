#include "main.h"

/**
 * _isupper - to test which character is upper
 * @c: this is the parameter
 * Return: int
 */

int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
